fastapi            # For building the FastAPI application
uvicorn            # ASGI server for running the FastAPI app
sqlalchemy         # For database ORM (if using SQLAlchemy)
mysql-connector-python  # MySQL driver for database connection
pydantic           # For data validation and settings management
python-dotenv      # For managing environment variables
jose               # For handling JSON Web Tokens (optional, if using authentication)
passlib[bcrypt]    # For password hashing (optional, if using authentication)
